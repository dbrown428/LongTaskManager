import {assert} from "chai";
// import {Promise} from "es6-promise";
import {LongTaskManagerConfiguration} from "../../../src/App/LongTaskManagerConfiguration";

describe("Long Task Manager Configuration", () => {
	it("Should instantiate a long task manager successfully", () => {
		// const manager = LongTaskManagerConfiguration.default();
		// assert.isNotNull(manager);
	});
});
